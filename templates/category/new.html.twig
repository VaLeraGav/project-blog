{% extends 'layout.html.twig' %}

{% block title %}New Category{% endblock %}

{% block body %}
    <h1>Create new Category</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
        <button class="btn btn-primary">Save</button>
    {{ form_end(form) }}


    <a href="{{ path('app_category_index') }}"><button class="btn btn-success">back to list</button></a>
{% endblock %}
